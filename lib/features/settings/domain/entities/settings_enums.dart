/// Measurement system used across the UI.
enum UnitSystem { metric, imperial }

/// Persisted theme preference.
enum ThemePreference { system, light, dark }

/// Biological sex, used for calorie and body-composition estimates.
enum BiologicalSex { unspecified, male, female }
